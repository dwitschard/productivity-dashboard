import { FC } from 'react';
import { HeroProps } from '@/components/landing-page/hero/hero';

const Helpers: FC<{}> = () => {
  return (
    <div>
      <div>Helpers</div>
    </div>
  );
};

export default Helpers;
